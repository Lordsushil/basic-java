package Controlstm;

public class controlstm {
	/*
	 *=================== Control statements ================================
	 * # use to implement conditions or logic in the program 
	 * 
	 *  # TYPES
	 *  
	 *  1> selection (Decision making)
	 *   a>if
	 *   b>if-else
	 *   c>nested if-else
	 *   d> if-else ladder
	 *   e> switch
	 *   
	 *  2> Looping (iteration)
	 *   a> for 
	 *   b> while
	 *   c> do-while
	 *   
	 *  3> Jumping (Branching)
	 *   a> break
	 *   b> continue
	 *   c> return
	 *
	 *
	 */
	
	
	

}
